import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('SmartSpend app smoke test', (WidgetTester tester) async {
    // Placeholder test — Hive requires initialization before running widget tests
    expect(1 + 1, equals(2));
  });
}
